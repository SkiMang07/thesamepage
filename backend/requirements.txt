# Security bumps 2026-09-24 (pip-audit clean at these pins). FastAPI is held at
# 0.136.3 on purpose: 0.137 wraps included routers, which hides every route
# from slowapi 0.1.9's middleware, and the default write limit then silently
# stops applying. tests/test_rate_limits.py fails if that happens.
fastapi==0.136.3
starlette==1.7.0
uvicorn==0.27.0
python-dotenv==1.2.3
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.27.2
supabase==2.9.1
pyjwt==2.13.0
cryptography==50.0.1
slowapi==0.1.9
python-multipart==0.0.32
# Text layer of an uploaded job description PDF (Roles & expectations), so the
# source pane and the no-invented-numbers guard read the real words.
pypdf==6.19.0
stripe==10.4.0
sentry-sdk[fastapi]==2.20.0
pytest==9.1.1
