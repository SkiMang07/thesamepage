// Blog index stub. This is the primary SEO/distribution surface for the
// content-led growth plan — posts should live as MDX or come from a
// headless CMS once there's real content, statically generated at build time.
export const metadata = { title: "Blog — The Same Page" };

export default function BlogIndexPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <h1 className="text-3xl font-semibold">Notes on managing people</h1>
      <p className="mt-4 text-gray-600">Posts coming soon.</p>
    </main>
  );
}
